version: '3.7'
services:
  sync:
    depends_on:
      - auth-mock
      - google-mock
      - postgres-auth
      - postgres-dird
      - rabbitmq
    environment:
      TARGETS: "auth-mock:9497 google-mock:80 postgres-auth:5432 postgres-dird:5432 rabbitmq:5672"
